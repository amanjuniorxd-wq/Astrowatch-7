# Astrowatch — Project Audit (MVP-2, Step 1)

Done before any new code, per instruction. Sandbox rechecked immediately before this
audit — still down, identical disk-space error as every prior attempt this session.
Nothing below required execution to assess; where execution *would* be needed to
confirm something, that's stated explicitly rather than assumed.

| File | Purpose | Current status | Known bugs | Untested functions | Dependencies | What must change |
|---|---|---|---|---|---|---|
| `coordinates.py` | RA/Dec → ecliptic transform, tropical + sidereal fields, source-metadata-preserving `PositionRecord` | Written; formula hand-verified once (Sun, 2026-08-12, ~8.6″/6″ agreement) via a round-trip design — never run as code | None identified, but "none identified" ≠ "verified" | All of them — `ra_dec_to_ecliptic`, `ra_dec_to_ecliptic_j2000`, `tropical_to_sidereal`, `build_position_record` | stdlib only (`math`, `dataclasses`, `typing`) | Needs `test_validation_suite.py` actually run. Separately: `self_test()` only *prints* the obliquity value, it doesn't assert anything — it looks like a test but isn't one. **HIGH** |
| `aspects.py` | Ptolemaic aspect detection (configurable orbs) + Bṛhat Saṃhitā graha-yuddha proximity classifier + tradition dispatcher + retrograde check | Written, not executed | `GRAHAYUDDHA_PLACEHOLDER_THRESHOLDS_DEG` are my own invented numbers, explicitly flagged — Ch. XVII gives no numeric degree cutoffs. `DEFAULT_ORB_DEG` for Ptolemaic aspects is likewise a modern-convention placeholder, not textually sourced. Neither is a "bug" exactly — both are honestly labeled — but both mean the rules that depend on them **cannot legitimately fire on real data yet**. **HIGH** | `is_retrograde()`, `detect_configuration()` | stdlib only | Needs real degree thresholds researched from secondary Sanskrit-astrology scholarship (not invented), or the rules stay blocked |
| `engines.py` | Three isolated tradition engines; refuses unresolved-zodiac rules; refuses cross-tradition application | Written, not executed | None identified — but it imports from all three other modules and has never actually been run, so even import-level correctness (typos, signature mismatches) is unconfirmed | Everything | stdlib only | Needs execution to confirm it even imports cleanly. **MEDIUM** |
| `ephemeris_client.py` | JPL Horizons client: OBSERVER (broken) + VECTORS (confirmed-working pattern) | Mixed — the VECTORS *URL pattern* was manually confirmed working via direct fetches this session; this specific `urllib`-based Python code implementing that pattern has never itself executed | **`fetch_all_classical_bodies()` calls `fetch_radec()`, which uses the OBSERVER endpoint — the one confirmed broken (empty responses, 3 separate manual attempts). This function would fail immediately if called.** **CRITICAL** | `fetch_ecliptic_vectors()`, `parse_vectors_xyz()`, `vectors_to_ecliptic_longitude()` — the actually-viable path — untested as code even though the underlying API calls were manually verified | stdlib only (`urllib`) | Fix `fetch_all_classical_bodies()` to use the VECTORS path, not OBSERVER. Then execute. |
| `pipeline.py` | Demo wiring | Not executed | None — but `demo_with_sample_positions()` uses hand-picked illustrative numbers, clearly labeled as such (not real data) | Both demo functions | stdlib only | Needs execution; not a priority relative to the others |
| `rule_registry.py` | Structured, cited rules (BS Ch. 16–20, 42; Ptolemy Book II Ch. III, VI) + `zodiac_requirement` auto-classification | Data-heavy, low execution risk | `BS-20`'s two rules are blocked at the *chapter* level even though neither `BS-20-02` nor `BS-20-sannipata`'s specific trigger actually needs a resolved zodiac position (only the separately-noted directional/compass component does) — over-conservative, not wrong, but coarser than the architecture intends. **MEDIUM** | `_apply_zodiac_requirements()`, `rules_for_tradition()`, `rule_by_id()` | stdlib only | Refine BS-20's classification to rule-level, not chapter-level, once there's time to do it carefully rather than as a rushed fix |
| `rule_matcher.py` | Matches configurations to rules; formats reports | Not executed | Inherits the graha-yuddha placeholder-threshold problem from `aspects.py` | `match_grahayuddha_rules()`, `match_eclipse_geography()`, `format_match_report()` | stdlib only | `match_lunar_pass_rules()` deliberately raises `NotImplementedError` (Ch. XVIII needs ecliptic-*latitude* data this project doesn't compute yet) — correctly stubbed, not a bug, but it's a real capability gap. **HIGH** |
| `test_validation_suite.py` | The actual automated multi-body/multi-date round-trip validation script | Written, complete, self-documented acceptance criteria — **never executed, even once** | N/A | The whole file | stdlib only | This is the single highest-leverage thing to run. Nothing downstream (backtest, statistics, forecast) means anything until this runs. **CRITICAL** |
| `ayanamsha.py` | Lahiri (Chitrapaksha) ayanamsha for MODERN/kundli use — LIVE Swiss Ephemeris query (mode 1 "Lahiri") as PRIMARY path, two-point linear model as an explicitly-flagged FALLBACK (see VALIDATION_REPORT.md Phases 7-9) | Written, not executed; `coordinates.py` delegates to this module; primary path is a real HTTP query to the actual SE reference implementation, not an approximation; methodology (mode 1, not 43/44/46) selected from source documentation before any accuracy comparison | Code itself never executed as Python (sandbox + 2 alternate agent-isolation paths all unreachable, re-confirmed this pass). `pyswisseph` could not be installed (no shell) so the live path depends on network + Astrodienst's public CGI tool, which has no documented production-API guarantee (see DEPENDENCIES.md). Fallback model's ~14-64 arcsec systematic error is unchanged from Phase 8 and still only used when the live path fails. **MEDIUM** | `lahiri_ayanamsha_deg()`, `fetch_live_swisseph_lahiri_ayanamsha_deg()`, `tropical_to_sidereal_lahiri()`, `cross_check()`, `methodology_status()` | stdlib only (`urllib`) | Install `pyswisseph` locally and swap the network call for `swisseph.get_ayanamsa_ut()` the moment a working shell is available — see `ayanamsha.py`'s "WHY NOT PYSWISSEPH" section |
| `rashi_nakshatra.py` | Rāśi (12-sign) + Nakshatra (27-mansion) classification layer, sits after the sidereal transform, before any rule engine | Written, not executed; boundary/wraparound logic hand-traced | Never executed. Epsilon-snap logic for 360°→0° float noise is a judgment call, not independently verified by a second implementation. **MEDIUM** | `rashi_for_longitude()`, `nakshatra_for_longitude()` | stdlib only | Run `tests/test_rashi_nakshatra.py` once an interpreter is available |
| `tests/test_ayanamsha.py` | Real `unittest` suite: 13 live-Swiss-Ephemeris-sourced test vectors (fallback model), live-query response-parsing tests (mocked, using real captured `swetest.cgi` text), fallback-wiring tests, + the source-inspection regression test guarding `coordinates.py` | Written, complete, every expected value hand-verified — **never executed** | N/A (same execution blocker as everything else) | The whole file | stdlib only (`unittest`, `unittest.mock`) | Run via `python3 -m unittest tests.test_ayanamsha -v` from `gmare/` once an interpreter is available |
| `tests/test_rashi_nakshatra.py` | Real `unittest` suite: Rāśi/Nakshatra/pada boundary tests, 360°→0° wraparound (incl. float-noise edge case), end-to-end integration with the sidereal transform | Written, complete, hand-traced — **never executed** | N/A | The whole file | stdlib only | Run alongside `test_ayanamsha.py` |
| `lahiri_crosscheck.csv` | Machine-readable 13-row cross-check table (fallback model vs. live Swiss Ephemeris, per-date diff + classification) | Complete, sourced, matches the numbers baked into `ayanamsha.py` (1950, 1975 added this pass) | None identified | N/A (data file) | N/A | Regenerate via `ayanamsha.cross_check()` if the reference dataset or model ever changes |
| `DEPENDENCIES.md` | Version/dependency record (SE version, pyswisseph N/A, selected mode, query mechanism) | Complete | None identified | N/A | N/A | Update `pyswisseph version` field once it's actually installed and used |
| `forecast.py` | EXPERIMENTAL forecasting CLI (unlocked this pass, explicit authorization) — rule-registry-only, geography/unresolved-rule/fallback safeguards | Written, not executed; pipeline logic hand-run against real live data for 2026-09-01/USA — see `FORECAST_RUN_2026_09_USA.md` (result: NO FORECAST, correctly) | Never executed as code. Depends on `ephemeris_client.py`, which itself has never executed. **HIGH** (forecasting is the highest-stakes surface in the project — everything upstream needs to actually run before this can be trusted beyond its current hand-verified state) | `run_forecast()`, `evaluate_rules()`, `geographic_specificity_for_rule()` | stdlib only | Run end-to-end once an interpreter is available; the CLI's `main()` currently refuses to run and points at the hand-executed report instead |
| `panchang.py` | Partial Panchang (tithi, vara, lunar nakshatra); yoga/karana explicitly NOT IMPLEMENTED | Written, not executed; tithi/vara hand-verified against the FINAL TASK example (vara cross-checked two independent ways: JD formula and calendar day-count from a known weekday) | Yoga/karana missing is a real capability gap, not a bug. **MEDIUM** | `compute_tithi()`, `compute_vara()` | stdlib only | Research yoga/karana definitions properly before implementing (do not guess a binning convention) |
| `predictions_schema.sql`, `evaluate_forecasts.py` | Prediction storage (immutable, DB-trigger-enforced) + manual (non-automated) forecast-vs-outcome evaluation | Written, not executed | N/A | Whole files | stdlib only (`sqlite3`) | Initialize the DB and exercise both files once an interpreter is available |
| `README.md` | Project overview | Current, accurately reflects NOT READY / LOCKED status | — | — | — | Keep updating as things change |
| `VALIDATION_REPORT.md` | Full validation record, phase-by-phase | Current, accurately reflects PARTIAL/NOT TESTED status, records 3 failed execution attempts | — | — | — | Keep updating |

## Severity summary

**CRITICAL -- RESOLVED as of the Astrowatch-2 port (see docs/ASTRONOMY_VALIDATION_REPORT.md Phase 11):**
1. ~~No code in this project has executed even once~~ -- code execution became available this pass. 45/45 unit tests executed and pass (test_ayanamsha.py, test_rashi_nakshatra.py); one genuine test-regex bug found and fixed by actual execution. rashi_nakshatra.py, panchang.py, aspects.py, rule_registry.py, engines.py, pipeline.py, predictions_schema.sql, evaluate_forecasts.py all executed with real, correct output. The live Swiss Ephemeris query and JPL Horizons fetch still cannot succeed IN THIS SANDBOX specifically (outbound proxy allowlist blocks astro.com/ssd.jpl.nasa.gov -- diagnosed via a direct curl test showing `X-Proxy-Error: blocked-by-allowlist`), so anything depending on those live calls remains PARTIALLY_VALIDATED, not VALIDATED --  see ayanamsha.methodology_status().
2. ~~`ephemeris_client.fetch_all_classical_bodies()` is built on the confirmed-broken OBSERVER endpoint~~ -- already fixed in a prior pass (code inspection confirms it now calls `fetch_ecliptic_vectors()`, the confirmed-working VECTORS path). This audit table was simply never updated to reflect that fix; corrected here. Executing it now fails cleanly with the same diagnosed proxy-allowlist block as everything else network-dependent in this sandbox -- not a code defect.

**HIGH (3):** `coordinates.py`'s `self_test()` doesn't assert anything; the graha-yuddha proximity thresholds are unsourced placeholders that block legitimate use of Ch. XVII; Ch. XVIII (Moon north/south) has no implementation at all, not just an untested one.

**MEDIUM (4):** BS-20 is over-conservatively blocked at chapter granularity; `engines.py`'s cross-module imports have never been confirmed to even load; `ayanamsha.py`'s live-query-primary/linear-fallback design is source-justified and cross-checked but has never itself executed as code, and depends on a public CGI tool with no production-API guarantee; `rashi_nakshatra.py`'s boundary logic is hand-traced but not independently verified by a second implementation or actual execution.

**LOW (2):** No `requirements.txt` exists (arguably fine — everything is stdlib-only by design, but the deliverable list asked for one, so it should exist and just say so). `panchang.py`, `historical_events` data, `backtest.py`, `historical_discovery.py`, `forecast.py` — none of these exist yet; not bugs, just the rest of MVP-2's scope, not started.
